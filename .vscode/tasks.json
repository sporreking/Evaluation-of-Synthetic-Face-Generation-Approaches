{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "${command:python.interpreterPath} run.py",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}