{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run",
            "type": "shell",
            "command": "conda run -n synthetic-face-generation --no-capture-output --live-stream python run.py",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}